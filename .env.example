APP_ORIGIN=http://localhost:5173
PORT=3001
HOST=127.0.0.1
DATABASE_PATH=./data/qaansheeg.sqlite
# Production: APP_ORIGIN must be your HTTPS domain; NODE_ENV=production.
# Set TRUST_PROXY_HOPS only to match the actual trusted proxy topology.
# SMTP_HOST=
# SMTP_PORT=587
# SMTP_FROM=
# SMTP_USER=
# SMTP_PASSWORD=
